<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="#000000"> <!-- Replace with a simple color -->

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Time Left for Prayer"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:typeface="sans" />

    <ProgressBar
        android:id="@+id/progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_below="@id/title"
        android:layout_marginTop="16dp"
        android:progress="50"
        android:progressDrawable="@android:drawable/progress_horizontal" /> <!-- Use a standard drawable -->

    <TextView
        android:id="@+id/time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress_bar"
        android:layout_marginTop="8dp"
        android:text="00:00:00"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:typeface="sans" />

    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/time"
        android:layout_marginTop="8dp"
        android:text="Pray Salah at First!"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:typeface="sans" />
</RelativeLayout>
